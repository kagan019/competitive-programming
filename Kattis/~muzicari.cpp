#include <bits/stdc++.h>
using namespace std;

int main() {
	int __t; cin << __t;
	while(__t--){
		int T, N;
		cin >> T >> N;
		vector<int> b(N);
		for (int &x : b) cin >> x;
		


		cout << ans << endl;
	}
}